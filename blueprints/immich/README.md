<!-- Generated by lib/render.py from sources/immich.json. Do not edit; `make` regenerates it and CI fails on drift. -->

# Immich

- `docker-compose.yml` -- the stack file this template deploys

## English

Photo and video library with automatic backup from phones, albums, sharing, face recognition and search by content.

- **Upstream project:** <https://immich.app>
- **Replaces:** **Google Photos**, **iCloud Photos**, **Amazon Photos**
- **Sign-in (SSO):** Enabled from the application's own admin screens: the `OIDC_*` values from the Environment tab are pasted in once.
- **Address:** `photos.yourdomain.com`, served from `immich-server:2283`

The address is attached when the template is deployed. A different one is
arranged beforehand, on request.

### Setup steps

1. *(Optional)* Keep the library on a separate disk: mount the disk on the server, create a directory on it (for example `/mnt/photos/immich`), and set `UPLOAD_LOCATION` to that path in the **Environment** tab. The directory has to exist before the deploy. Then add the same path under **Settings** -> **Backup** -> extra paths in catena-admin, so the nightly backup includes it.
2. Click **Deploy**. The first boot takes 2 to 3 minutes while the database initialises.
3. Visit the Immich domain and create the first account, which becomes the administrator.
4. *(Optional)* Enable Keycloak SSO: **Administration** -> **Settings** -> **Authentication Settings** -> **OAuth**, then enter:
   - **Issuer URL:** `OIDC_ISSUER_URL` from Environment
   - **Client ID:** `OIDC_CLIENT_ID` from Environment (`immich`)
   - **Client Secret:** `OIDC_CLIENT_SECRET` from Environment (minted in Keycloak on request when blank)
   - Save. The login page gains a **Login with OAuth** button. The local administrator keeps working as a break-glass.
5. Install the Immich app on each phone, enter the Immich domain as the server address, and turn on backup.

### Where the library lives

By default the library is a named volume under `/srv/catena`, which the nightly backup already covers. Setting `UPLOAD_LOCATION` to an absolute path moves it to that directory instead, which is the way to use a larger disk mounted on the server. A path outside `/srv/catena` is backed up only once it is declared in catena-admin under **Settings** -> **Backup** -> extra paths; until then the nightly coverage check reports it as data outside the backup set.

### Machine learning

Search by content, face recognition and duplicate detection run in the `immich-machine-learning` service. It downloads its models on first use and holds them in memory while they are in use. On a server with little memory, the features can be turned off under **Administration** -> **Settings** -> **Machine Learning Settings**.

### Environment variables

These are the fields the deploy form presents in the **App Templates**
panel. Random secrets are minted on the host when the template is first
seeded, so none of them has to be generated by hand.

| Variable | Default |
|---|---|
| `DOMAIN_HOST` | `photos.yourdomain.com` |
| `IMMICH_HOSTNAME` | `photos.yourdomain.com` |
| `DB_PASSWORD` | _auto-generated random value_ |
| `UPLOAD_LOCATION` | `immich-library` |
| `OIDC_CLIENT_ID` | `immich` |
| `OIDC_CLIENT_SECRET` | _set before deploy_ |
| `OIDC_ISSUER_URL` | `https://auth.yourdomain.com/realms/catena` |

---

## Français

Bibliothèque de photos et vidéos avec sauvegarde automatique depuis les téléphones, albums, partage, reconnaissance des visages et recherche par contenu.

- **Projet original :** <https://immich.app>
- **Remplace :** **Google Photos**, **Photos iCloud**, **Amazon Photos**
- **Connexion (SSO) :** À activer depuis les écrans d'administration de l'application : les valeurs `OIDC_*` de l'onglet Environment y sont collées une fois.
- **Adresse :** `photos.yourdomain.com`, servie par `immich-server:2283`

L'adresse est attachée au déploiement du modèle. Une autre se convient au
préalable, sur demande.

### Étapes de configuration

1. *(Optionnel)* Placez la bibliothèque sur un disque séparé : montez le disque sur le serveur, créez-y un répertoire (par exemple `/mnt/photos/immich`) et indiquez ce chemin dans `UPLOAD_LOCATION` dans l'onglet **Environment**. Le répertoire doit exister avant le déploiement. Ajoutez ensuite le même chemin dans catena-admin sous **Settings** -> **Backup** -> chemins supplémentaires, pour que la sauvegarde nocturne l'inclue.
2. Cliquez **Deploy**. Le premier démarrage prend 2 à 3 minutes, le temps d'initialiser la base de données.
3. Visitez le domaine Immich et créez le premier compte, qui devient l'administrateur.
4. *(Optionnel)* Activez Keycloak SSO : **Administration** -> **Settings** -> **Authentication Settings** -> **OAuth**, puis renseignez :
   - **Issuer URL :** `OIDC_ISSUER_URL` depuis Environment
   - **Client ID :** `OIDC_CLIENT_ID` depuis Environment (`immich`)
   - **Client Secret :** `OIDC_CLIENT_SECRET` depuis Environment (généré côté Keycloak sur demande lorsqu'il est vide)
   - Validez. La page de connexion affiche **Login with OAuth**. L'administrateur local continue de fonctionner comme issue de secours.
5. Installez l'application Immich sur chaque téléphone, indiquez le domaine Immich comme adresse du serveur et activez la sauvegarde.

### Emplacement de la bibliothèque

Par défaut, la bibliothèque est un volume nommé sous `/srv/catena`, déjà couvert par la sauvegarde nocturne. Indiquer un chemin absolu dans `UPLOAD_LOCATION` la place dans ce répertoire, ce qui permet d'utiliser un disque plus grand monté sur le serveur. Un chemin hors de `/srv/catena` n'est sauvegardé qu'une fois déclaré dans catena-admin sous **Settings** -> **Backup** -> chemins supplémentaires ; d'ici là, la vérification nocturne de couverture le signale comme données hors de la sauvegarde.

### Apprentissage automatique

La recherche par contenu, la reconnaissance des visages et la détection des doublons tournent dans le service `immich-machine-learning`. Il télécharge ses modèles à la première utilisation et les garde en mémoire pendant leur usage. Sur un serveur avec peu de mémoire, ces fonctions se désactivent sous **Administration** -> **Settings** -> **Machine Learning Settings**.

### Variables d'environnement

Ce sont les champs présentés par le formulaire de déploiement du panneau
**App Templates**. Les secrets aléatoires sont générés sur l'hôte au
premier semis du modèle : aucun n'est à produire à la main.

| Variable | Valeur par défaut |
|---|---|
| `DOMAIN_HOST` | `photos.yourdomain.com` |
| `IMMICH_HOSTNAME` | `photos.yourdomain.com` |
| `DB_PASSWORD` | _valeur aléatoire auto-générée_ |
| `UPLOAD_LOCATION` | `immich-library` |
| `OIDC_CLIENT_ID` | `immich` |
| `OIDC_CLIENT_SECRET` | _à définir avant déploiement_ |
| `OIDC_ISSUER_URL` | `https://auth.yourdomain.com/realms/catena` |
