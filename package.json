{
  "name": "@catenahq/templates",
  "version": "0.2.0",
  "private": true,
  "description": "Catena template catalog: one JSON source per template plus its compose file, rendered into Portainer App Templates (templates.json v3) and the machine catalog (catalog.json). Consumers read main, fetched raw.",
  "type": "module",
  "scripts": {
    "render": "uv run build/render.py",
    "validate": "uv run build/validate.py",
    "verify": "uv run build/render.py && git diff --exit-code blueprints templates.json catalog.json index.html",
    "check:unicode": "node ../contracts/scripts/check-unicode.mjs",
    "check:prose": "node ../contracts/scripts/check-prose.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/catenahq/catena-templates.git"
  },
  "license": "SEE LICENSE IN LICENSE"
}
