{
  "name": "@catenahq/templates",
  "version": "0.1.0",
  "private": false,
  "description": "Catena template catalog: per-template compose files + metadata + render pipeline emitting Portainer App Templates (templates.json v3). Consumed by catenahq/ops (vendored tarball) and Portainer directly (App Templates URL).",
  "type": "module",
  "files": [
    "source"
  ],
  "scripts": {
    "render": "uv run build/render.py",
    "verify": "uv run build/render.py && git diff --exit-code blueprints templates.json",
    "check:unicode": "node scripts/check-unicode.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/catenahq/templates.git"
  },
  "license": "SEE LICENSE IN LICENSE"
}
